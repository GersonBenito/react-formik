import React, { useState } from 'react';

/**
 * @param {*} initialState campos que trandra el formulario
 */
const BForm = ({initialState}) => {

    const [ formulario, setFormulario ] = useState(Object.keys(initialState));

    return (
        <div>
            
        </div>
    )
}

export default BForm
